# Plan: mini-SIEM implementation — PTK-maintained OTLP receiver shipped as a separate product

## Status and authority

- **Document status: APPROVED — owner approved this plan as reviewed, in-session, 2026-07-15** (codex review loop complete: 3 rounds; round-3 verdict resolved 8/10 prior findings; the remaining 2 — msi-7, msi-15 — and the 5 new round-3 findings — msi-20…msi-24 — were remediated in this document after the final round, without further codex re-review; the post-loop remediation and its recorded tradeoffs were reviewed and approved by the owner as part of this approval). **S1-S7 are complete as of 2026-08-12.** Audit restoration executed S4a/S5/S6; the evidence reconciliation at `9ad7d9f` identified the missing S4b durability/custody work, which subsequently landed and closed cr10/cr11/cr12 before S7. S8 packaging remains a separately gated follow-up.
- **Corrective S3H amendment status: COMPLETE at `c726a33` — owner approved the amendment as proposed, in-session, 2026-07-16.** S1-S3 were already complete but had not implemented the startup filesystem enforcement required by Architecture and acceptance row 7 even though that row assigned it to S1/S3. S3H is confined to `siem/`; exact cross-platform and mutation evidence is recorded in `.agents/machines.md`. At S3H landing, S4-S6 were still held behind the then-planned v3 protobuf fixture. That historical gate was later amended by audit restoration and is superseded by the S4a/S4b status above.
- The underlying decision was OPEN (`.agents/decisions.md:298-340`, appended by owner 2026-07-14, "No implementation is authorized" at `:300-302`).
- On 2026-07-15 the owner directed this session to move the mini-SIEM from R&D to planning and implementation, selected Option 1 (PTK-maintained receiver) with an expanded product scope, and answered the scope questions recorded in "Product decisions" below. This plan is the implementation counterpart the discovery plan's D5 gate anticipated (`.agents/plans/mini-siem-discovery.md:146-148`).
- The owner's instruction waives the D1 candidate-survey comparison (discovery plan `:99-112`): the standing recommendation to "compare the smallest existing durable OTLP deployment against a custom receiver" (`.agents/decisions.md:337-340`) is superseded by the owner's direct product decision to build.
- Slice S0 below drafts the decision-entry update. Per the reconciliation hold (`mini-siem-discovery.md:154-155`), only the owner lands edits to `.agents/decisions.md`; code slices S1+ are gated on that entry landing (or the owner explicitly delegating the append).

## Provenance

- Drafted 2026-07-15 on branch `plan/mini-siem-discovery`, worktree `.claude/worktrees/mini-siem`, cut from `master` @ `5ae154c`.
- Predecessor: `.agents/plans/mini-siem-discovery.md` (3-round codex review loop, findings msp-1..msp-9 all resolved).
- The resilience compatibility contract is consumed read-only from `git show 33d6a35:.agents/plans/mcp-resilience.md` (SIEM-relevant lines 34-37, 561-584, 686-690, 778-805, 845-880). This plan tracks that contract as it lands on master and never forks or restates it authoritatively.

## Binding inputs (inherited verbatim from discovery — not re-litigated)

1. **No new SIEM transport.** The receiver implements the exact authenticated one-record OTLP/HTTP protobuf request/ack/retry contract PTK already ships (`33d6a35` plan lines 561-584; `server/AUDIT-EXPORT.md`).
2. **Contract prohibitions 25-27** (`33d6a35` lines 853-856): never change the OTLP wire/envelope by destination or schema version; never drop or mistype a prior or `ptk.host.*` attribute; never let a receiver-side ack replace PTK's configured durable OTLP anchor semantics.
3. **Anchor boundary** (`server/AUDIT-EXPORT.md`): ack only after durable commit under a separately administered principal. A same-user sidecar or in-memory collector is not a meaningful anchor. A valid ack is the full valid/nonrejecting OTLP response per `server/AUDIT-EXPORT.md:87-97` — a bare `200` is not an ack.
4. **Conformance seam** (`33d6a35` lines 799-801; discovery `:50-54`): the mini-SIEM conformance fixture must be able to replace the resilience plan's fake durable receiver **without changing PTK bytes**.
5. **The four typed v3 attributes** (`33d6a35` lines ~567-575): `ptk.host.boot_id: string` (omitted when null), `ptk.host.generation: int64` (omitted when null), `ptk.host.state: string` (always present), `ptk.host.recovery_attempt: int64` (always present).

## Product decisions (owner, 2026-07-15 session)

| Topic | Decision |
|---|---|
| MVP surface | Receiver + durable store + query + alert rules + web dashboard |
| Storage | SQLite (`Microsoft.Data.Sqlite`), with receiver-side hash-chain tamper evidence in rows |
| Dev home | New top-level `siem/` directory with its own solution; separate product box |
| Code sharing | Keep the vendored proto receiver-owned at `siem/PtkSiemReceiver/Protos/audit_otlp.proto` and share only golden v1/v2/v3 fixture bytes; no shared runtime code |
| Dashboard | ASP.NET Core minimal API + embedded static htmx pages; no node toolchain |
| Platforms | Cross-platform (ubuntu/windows/macos); MSI as optional Windows component in a later packaging phase; tar/zip elsewhere |

## Architecture

New solution `siem/PtkSiem.slnx` (house `.slnx` format), all projects `net10.0`, `Nullable` + `ImplicitUsings` enable, no analyzers/AOT (matches `server/` conventions):

- **`siem/PtkSiemReceiver/PtkSiemReceiver.csproj`** (`Microsoft.NET.Sdk.Web`, Exe). Generic Host + `BackgroundService` composition mirroring `server/PtkMcpServer/Program.cs`. Two Kestrel endpoints:
  - **Ingest endpoint** (mTLS required): `POST /v1/logs`, OTLP/HTTP protobuf only (`Content-Type: application/x-protobuf`). Frozen client-cert policy per ingest listener: Kestrel `ClientCertificateMode.RequireCertificate`; chain validation via `X509ChainPolicy` with custom root trust pinned to the configured CA **bundle** (multiple CAs allowed so old+new overlap during rotation); clientAuth EKU + validity window enforced; revocation-check mode explicit in config, no silent fallback. S2 negative tests on all three OSes: missing cert, wrong CA, expired, wrong EKU, revocation-policy mismatch, old/new rotation pairs.
  - **Operator endpoint** (separate port, loopback-bound by default): query API + dashboard. A non-loopback operator bind is a **startup failure** unless an operator HTTPS certificate is configured — the bearer token never travels plaintext off-host (startup validation + network integration test, S1/S5).
- **Wire layer**: compiles the receiver-owned canonical proto via `<Protobuf Include="Protos/audit_otlp.proto" GrpcServices="None" />` with `Google.Protobuf` 3.35.1 + `Grpc.Tools` 2.83.0 (`PrivateAssets=all`), independently of the PTK runtime and with no `OpenTelemetry.*` NuGet. The tools pin was raised from 2.82.0 after the signed five-RID draft exposed upstream's known Linux ARM64 `protoc` exit-139 regression; upstream fixed that regression in 2.83.0.
- **Ingest pipeline** (per request, in order):
  1. Parse `ExportLogsServiceRequest`; malformed/oversized ⇒ HTTP 400, never an ack. Request size cap configurable (default 1 MiB).
  2. Extract the single log record (producer sends one record per request); validate schema version (v1/v2/v3 accepted), retained attribute names/types, and the four `ptk.host.*` rules.
  3. **Strict record validation** (independent re-specification, no PTK runtime code imported): recompute `event_hash` as SHA-256 over the exact reconstructed pre-hash bytes (the `AuditSpoolRecordCodec` envelope semantics), verify exact envelope fields, canonical UUID forms, sequence rules keyed by `producer.supervisor_boot_id`, expected next sequence + `prev_hash` against the stored chain head, and equality between body values and every projected OTLP attribute — one mutation test per check (S4). Two carve-outs are evaluated **before** chain-position rejection: (a) a byte-identical duplicate of an already-committed row (event ID + `event_hash` match) short-circuits to check 4's idempotent success ack before any next-sequence/head comparison — a legitimate at-least-once replay is by definition no longer "next" and must never be chain-rejected (S4 test: commit N, let the head advance, replay N byte-identically ⇒ idempotent `200` ack, not `400`); (b) while a gap for the record's `producer.supervisor_boot_id` is `open`, an otherwise-valid record whose sequence lies beyond the gap is not `400`-rejected — it is committed flagged `post-gap` with `prev_hash` continuity enforced against the previous stored post-gap record for that boot (the first post-gap record anchors the sub-chain at the gap boundary) and acknowledged with the frozen `200` success ack, pending operator disposition — this is the storage path the S6 gap-resume protocol depends on. Every other failure ⇒ permanent rejection (HTTP `400`, see response table), never an ack. Before the `400` is written, a rejected-attempt/quarantine row is committed durably in its own transaction (attempt evidence, claimed hashes, implied gap) so chain-break/gap alerting has durable input without acknowledging the invalid record.
  4. Duplicate detection by event ID + hash — evaluated **before** chain-position/next-sequence rejection (the numbering is presentation order, not evaluation order): identical replay ⇒ idempotent success ack without a second row (at-least-once delivery, discovery matrix row 3), even after the chain head has advanced past it; same event ID with different bytes ⇒ rejection.
  5. **Durable commit**: one serialized SQLite transaction (`BEGIN IMMEDIATE`) that re-reads and re-validates the stored chain head *inside* the transaction (step 3's earlier read is advisory only), inserts the raw request bytes + extracted columns, and conditionally advances the exact prior head — chain-head validation and insertion are never split across transactions; a unique `(boot_id, sequence)` constraint backstops fork prevention. `PRAGMA journal_mode=WAL; PRAGMA synchronous=FULL;` so the commit is fsync-durable before the response is written. Fork test (S3): two simultaneous candidates for the same next sequence — exactly one event commits, the loser is rejected/quarantined.
  6. **Ack**: only after the transaction commits, write the frozen success response: HTTP `200`, parameter-free `Content-Type: application/x-protobuf`, body = serialized empty `ExportLogsServiceResponse` with `partial_success` unset (the valid nonrejecting ack of `server/AUDIT-EXPORT.md:87-97`, recognizable by PTK's exporter classifier, `server/PtkMcpServer/Audit/AuditOtlpHttpExporter.cs:248`).
  - Disk-full / `SQLITE_FULL` / any commit failure or backpressure ⇒ HTTP `503` + protobuf `google.rpc.Status` + `Retry-After` — **never a false 200** (discovery `:132-134`). 503 is the transient signal PTK's existing retry schedule honors.
  - **Frozen response table** (every row tested against PTK's real exporter classification, S2): `200` = the exact success ack in step 6, nothing else; `400` + protobuf `google.rpc.Status` = permanent single-record validation failure (malformed, schema violation, strict-validator failure, mismatched duplicate); `503` + protobuf `google.rpc.Status` (+ `Retry-After`) = transient storage/backpressure failure. Because each PTK request carries exactly one record, whole-request rejection uses HTTP error codes per the [OTLP spec](https://opentelemetry.io/docs/specs/otlp/); `partial_success` is never used in this contract.
- **Store** (`siem/PtkSiemReceiver`, `Storage/`): SQLite file under a configurable data root (through S3, the configured SQLite file's immediate parent is the data root). Tables: `events` (unique event ID, a unique `(boot_id, sequence)` composite constraint, schema version, raw protobuf bytes, event hash, prev hash, received UTC, extracted query columns + indexes), `chains` (per-boot chain head state), `gaps`/`alerts`, `meta` (receiver identity, schema migration version). Raw bytes are the evidence of record; extracted columns are derived and rebuildable. **Receiver custody ledger** (the owner-required receiver-side hash chain, distinct from PTK's producer chain): a versioned `custody` table — receipt sequence, prior receipt hash, SHA-256 over a deterministic framing of the exact raw request bytes + immutable receiver metadata (receipt UTC, client-cert thumbprint, remote endpoint, disposition) — written atomically in the same transaction as the event insert; retention tombstones, quarantine rows, and alert lifecycle records (creation, acknowledgement/state change, deletion — each carrying rule/config identity, written in the alert-persistence transaction) chain through it. PTK's `event_hash`/`prev_hash` cover only the producer's source JSON body; the custody ledger is what makes receiver-side tampering (rows, alerts, tombstones) evident. Mutation tests cover raw bytes, metadata, tombstones, alert deletion/modification, and quarantine mutation (S4). **Independent custody witness** (a hash chain alone cannot detect whole-store rewrite or tail truncation — `server/AUDIT-EXPORT.md:251-254`): the receiver appends custody-head checkpoints (receipt sequence + head hash + UTC) at every clean shutdown and on a configurable cadence to an append-only checkpoint location **outside** the SQLite data root with stricter, separately administered ownership (startup-validated like the other protected paths), The local checkpoint stream is receiver-written, so its guarantee is explicitly bounded: it detects store tampering by any actor lacking write access to the checkpoint location, and whole-store rewrite or tail truncation by anyone who cannot also forge checkpoints — it cannot, by itself, defeat an adversary holding the receiver identity who rewrites store and subsequent checkpoints together, and an unwitnessed suffix (records newer than the last checkpoint) is protected only up to the checkpoint cadence. That residual tier requires off-box anchoring: when configured (webhook or write-once file drop), the receiver ships every checkpoint head off-box; S7 install docs make configuring an anchor the recommended default and specify the manual alternative (operator records the published head out-of-band on a fixed cadence), and the S7 threat-model table states plainly that coordinated store+checkpoint rewrite and unwitnessed-suffix loss by a receiver-identity adversary are detected **only** with an off-box anchor or manual attestation in place. Startup and periodic verification recompute the ledger against the newest retained checkpoint (and the newest anchored head when off-box anchoring is configured) and alarm on any mismatch or head regression. S4 must include full-database rewrite, tail-truncation, and checkpoint-regression tests that alarm. No checkpoint or file-drop-anchor path exists through S3; the later slice that introduces either path must route it through S3H's protected-path boundary before its first read or write and extend S3H's negative matrix. S3H does not add witness configuration or behavior.
- **Query API + dashboard** (`Web/`): read-only minimal-API endpoints (events by time/type/session/boot filters, event detail with chain context, chain status, gap and alert lists) rendered by embedded static htmx pages, plus the **alert-lifecycle API** — the sole writer of alert custody events: authenticated endpoints to acknowledge/annotate/close an alert with explicit transition rules (`open → acknowledged → closed`; no other transitions; alert rows are never deleted by this API — closure is a state change, and the only alert-row deletion is retention-driven and itself custody-recorded), each transition committed in the same transaction as its custody-ledger entry recording who/when/prior-state. S6 tests drive every legal transition and reject every illegal one; S4 mutation tests tamper with a persisted transition record and must detect it. Operator auth: bearer token from the config file (0600-perm), endpoint loopback-bound unless explicitly configured otherwise — read authorization distinct from the ingest path (matrix rows 7/8).
- **Alert rules** (`Alerting/`): declarative rules in the config file — event-type/attribute match, chain-break, gap-detected, ingest-rate threshold. Chain-break/gap alerts are fed by the durable quarantine/gap rows written at rejection time (see ingest pipeline step 3), so a rejected record still produces a durable alert. A receiver-local, separately administered gap-resolution state machine governs resumption after PTK's operator gap disposition (`server/AUDIT-EXPORT.md:176`): states `open → dispositioned(resolved | accepted-loss) → resumed`, persisted on the `gaps` row and chained through the custody ledger; the sole transition authority is an authenticated operator API call (who/when/disposition recorded); resume boundary — while `open`, later valid records for that boot are stored but flagged `post-gap` (never silently re-anchored), and on disposition the chain resumes from the first stored post-gap record; resolution never acknowledges the invalid record and preserves the missing-hash evidence. S6 test: reject → operator disposition → restart → resume, proving the state survives restart. Actions: dashboard alert list + optional webhook POST with retry. Evaluation is decoupled from the ack path but **crash-safe**: the same transaction that commits a quarantine/gap/event row durably enqueues an alert-evaluation work item stamped with the SHA-256 of the startup-frozen rule configuration active at enqueue time; a persisted evaluation cursor plus idempotent startup replay guarantees every committed work item eventually yields its alert exactly once, and replayed items evaluated after a restart record both config hashes (enqueue-time and evaluation-time) on the resulting alert so a rule change across a crash is evident, never silent — the S6 kill test asserts the recorded config identity on the post-restart alert. S6 kill test: crash after ingest/quarantine commit but before alert persistence — the alert exists after restart.
- **Retention** (`Storage/RetentionService`): age/size-based purge as a `BackgroundService`; retention settings in config (matrix row 8). **Frozen tombstone commitment** (schema versioned with the store): each purge writes — atomically in the same transaction as its deletes, with rollback on partial failure — a tombstone row committing to the purged per-boot sequence range, boundary hashes (last retained `prev_hash`/`event_hash` on each side), purged-record count, a deleted-record commitment (rolling SHA-256 over the purged records' `event_hash` values in sequence order), and custody predecessor/successor linkage, so producer-chain and custody verification succeed over retained history without the raw bytes. Tests (S4): purge → restart → full chain + custody verification passes; tombstone boundary mutation alarms; partial-purge rollback leaves the store verifiable.
- **Config**: env var `PTK_SIEM_CONFIG` → one validated JSON file, frozen at startup, modeled on `AuditExportConfigurationLoader`/`AuditStartupConfiguration` (`server/PtkMcpServer/Audit/AuditExportConfiguration.cs`, `AuditStartupConfiguration.cs`): ingest bind address, server certificate + key, client CA, operator bind address + token, SQLite path, retention, alert rules. Strict validation, actionable failures, no fallback defaults for security-relevant fields.
- **Separate principal + off-host custody** (matrix rows 1/7; `server/AUDIT-EXPORT.md:205`; `.agents/decisions.md:304`): an **anchored** deployment requires a distinct receiver host (or equivalently isolated machine) under a separately administered principal — records must leave the PTK source machine. Same-host installs, including under a second OS account, are documented as development/non-anchoring (`PtkAuditAdmin`-style same-user layouts likewise). The receiver runs as its own OS account with data root and config owned by that account. **Startup enforcement, fail closed** (the `server/AUDIT-EXPORT.md:48` precedent): config, database, `-wal`/`-shm` sidecars, data directory, the custody checkpoint location once introduced, **every referenced TLS material file** (ingest server certificate + private key, client CA bundle(s), operator HTTPS certificate + key when configured), and the immediate parents of all of these must be owner-only. On POSIX, the effective process user owns each protected path, directories are exactly `0700`, and files are exactly `0600`; macOS extended ACLs that grant another principal are refused. On Windows, the current receiver SID owns each path and its protected DACL contains exactly one non-callback, non-inherited allow ACE granting that SID `FullControl`. Existing config, TLS, database, sidecar, and directory objects are verified without permission repair; receiver-created database and sidecar objects are created/protected and then verified. Every existing lexical path component is checked without following it and any symlink, junction, mount-point reparse point, or other reparse redirect is refused. Enforcement completes before either listener starts, with negative tests on every OS (including permissive mode/DACL, link/reparse, and replaced/swapped cases for each config/PEM role), not documentation-only. Install docs (S7) cover Windows service account, and dedicated users on macOS/Linux.
- **`siem/PtkSiemReceiver.Tests/PtkSiemReceiver.Tests.csproj`**: xUnit 2.9.3 / runner 3.1.4 / Test SDK 17.14.1 / coverlet 6.0.4, `<Using Include="Xunit" />`, flat `*Tests.cs` files, nested `private sealed class XxxFixture : IDisposable`, `[CollectionDefinition(..., DisableParallelization = true)]` for process/port tests — the `server/PtkMcpServer.Tests` house style.

## Slices

- **S0 — Decision entry + governance.** Draft the decision-entry update (below) for the owner to land in `.agents/decisions.md`; commit this plan. Code slices are gated on **both** the entry landing and the owner approving this plan as reviewed (post codex loop).
- **S1 — Skeleton.** `siem/PtkSiem.slnx`, receiver + tests projects, config loader with validation tests, CI job (`dotnet test siem/PtkSiem.slnx` added to `.github/workflows/ci.yml` across the existing three-OS matrix). No PTK files modified.
- **S2 — OTLP ingest surface.** Proto compilation, Kestrel mTLS ingest endpoint (frozen client-cert policy above + certificate negative matrix), request parsing/validation, and the frozen response table (200 ack / 400 permanent / 503 transient, size caps). Every response row is verified as PTK-recognizable via the conformance seam (binding input 4), which requires **one authorized, additive, test-only producer commit** (owner-approved; see the Non-goals carve-out): today the producer tests instantiate `FakeOtlpHttpsReceiver` directly (`server/PtkMcpServer.Tests/AuditOtlpHttpExporterIntegrationTests.cs:12`) and `PtkMcpServer` friend access excludes SIEM tests, so no seam exists without it. That commit adds an opt-in endpoint override (env-var-gated xUnit fixture parameter) letting the existing producer exporter tests target this receiver in place of the fake durable receiver — no production runtime changes, identical behavior when the override is unset. The classifier of record is `server/PtkMcpServer/Audit/AuditOtlpHttpExporter.cs`, never a re-implementation. Tests drive the endpoint with real client certs from a `FakeOtlpPki`-style helper (pattern from `server/PtkMcpServer.Tests/FakeOtlpHttpsReceiver.cs`; that fake disables client-cert validation — the SIEM helper enables and validates it).
- **S3 — Durable store.** SQLite schema + migrations (events + custody ledger), durable-before-ack commit ordering, duplicate idempotence, strict-validator + quarantine paths, disk-full/commit-failure rejection paths, crash-consistency tests. Every writer connection asserts effective `journal_mode=wal` and `synchronous=2` (FULL) at open — app-crash durability holds in any synchronous mode; FULL is what covers OS-crash/power-loss, so it is verified directly via PRAGMA rather than claimed from a kill test.
- **S3H — Startup filesystem hardening (approved corrective slice).** Implement the S3H contract below entirely under `siem/`. This closes the unscheduled hardening for every path that exists through S3 while leaving custody-checkpoint enforcement with the later slice that introduces that currently nonexistent path. S3H neither changes PTK nor begins S4-S6.
- **S4a — Producer/receiver conformance (EXECUTED as audit-restoration R5a).** The later audit-restoration rulings amend the original fixture gate: canonical producer-owned **v1/v2** corpora replace v1/v2/v3 (`audit-restoration-r1-discovery.md` §4), and the producer's real OTLP/HTTP JSON encoder replaces protobuf (`audit-restoration-r1-discovery.md` §3 amendment). `SiemConformanceGoldenTests` owns OTLP/HTTP JSON and Splunk HEC v1/v2 golden requests; `ProducerConformanceTests` consumes the exact OTLP bytes, fails closed when they are absent or outside this checkout, and proves acceptance, byte-identical evidence storage, Unicode/timestamp fidelity, and idempotent replay. This completes only the fixture/conformance half of the former S4 text.
- **S4b — Durability barriers + verifiable custody (EXECUTED 2026-08-12; cr10/cr11/cr12 closed).** Complete every unlanded acceptance property from the former S4 before S7 or receiver release readiness:
  1. **Real process barriers (IMPLEMENTED 2026-08-12; cr12 clean).** A separate test-only receiver host supplies a deterministic pre-commit hold while the positive post-ack/restart path launches the shipped standalone receiver entry point. Killing either whole process tree before commit yields no valid nonrejecting ack and no committed event/chain/custody state after restart. After an observed valid ack, an immediate whole-tree kill and production restart serves the exact request/body with intact producer chain and healthy witnessed custody; replay remains one event/chain/receipt.
  2. **Independent discrimination (IMPLEMENTED 2026-08-12; cr12 clean).** A deterministic ack-before-commit decorator waits until the real SQLite transaction is held immediately before commit, returns a valid ack, and is then killed; the post-ack proof rejects it because restart returns no event. A `synchronous=OFF` fault mutant is rejected by the effective writer-policy assertion. Moving the hold after `Commit()`, substituting the early-ack double into the positive proof, and removing the FULL comparison each independently failed its named guard. Existing in-process interruption and positive `synchronous=FULL` tests remain supporting evidence only.
  3. **Custody verifier (IMPLEMENTED 2026-08-12).** Schema v8/v2 receipts retain an independently recomputable evidence digest and exact evidence while live. Startup, periodic health, and the store verifier enforce contiguous receipt sequence, predecessor links, receipt/evidence hashes, live event/quarantine bytes, latest gap/alert lifecycle state, restore evidence, and tombstone integrity. Legacy v1 source evidence is backfilled where exact bytes remain and otherwise counted explicitly. Mutation guards cover event bytes, receipt metadata, alert state, restore state, and tombstone boundaries.
  4. **Retention tombstones (IMPLEMENTED 2026-08-12; cr10 closed at its two-round review cap).** Event, quarantine, and closed-alert retention atomically writes the versioned commitment before deletion: per-boot ranges and boundaries for events, purged count, evidence-digest commitment, deleted receipt entries, and custody predecessor/successor linkage. Exact evidence bytes compact only after the tombstone receipt lands in the same transaction. Verification remains possible after source rows are removed; an injected pre-commit interruption proves sources, receipt evidence, and tombstones all roll back.
  5. **Independent witness (IMPLEMENTED 2026-08-12; cr11 closed at its two-round review cap).** Immutable hash-chained custody-head checkpoint files (sequence, head hash, UTC) land outside the SQLite data root on cadence and clean shutdown through S3H's protected-path boundary. Startup and periodic checks detect whole-store rewrite, custody-tail truncation, checkpoint regression, and off-box anchor regression. An optional second protected write-once file-drop directory is anchor-first and recovers a crash-left anchor-ahead record; the authenticated health API exposes the latest witness hash for manual out-of-band attestation. Ingest, alert evaluation, and retention pause on unhealthy custody.
  6. **Restore reconciliation (IMPLEMENTED 2026-08-12; cr11 closed at its two-round review cap).** The online-backup → additional-ingest → older-backup-restore guard proves startup preserves witnessed history and pauses mutations. Authenticated `POST /api/custody/restore` appends a branch/restore witness record, atomically custody-records the restore in schema v10, raises exactly one open `custody_restore_data_loss` alert requiring normal disposition, checkpoints the reconciled branch, and permits replay. Restart does not duplicate restore evidence or alerts.
  7. **Mutation matrix and verification (IMPLEMENTED; all section reviews closed).** Each evidence class above has a biting mutation test, including tombstone boundaries, alert/quarantine mutation, whole-database rewrite, tail truncation, checkpoint regression, wrong-side commit hold, ack-before-commit, and `synchronous=OFF`. Restored full SIEM verification passes 329/329; standing server/Pester/dependency verification passes. cr12's first valid Claude Code 2.1.228 / claude-opus-5 / xhigh generation round returned clean over exact `1be29fc..9b14a7e`, so the codereview playbook stopped without consuming the optional second round. No PTK production or wire-contract change belongs in S4b.
- **S5 — Query API + dashboard (EXECUTED as audit-restoration R5b).** Read-only endpoints + embedded pages, operator token auth, loopback default. Endpoint handler tests + end-to-end ingest→query coverage.
- **S6 — Alerts (EXECUTED as audit-restoration R5c).** Rule evaluation over the durable work-item queue + persisted evaluation cursor, alert persistence (with custody-ledger lifecycle entries), webhook action with bounded retry, dashboard surfacing, and gap disposition.
- **S7 — Ops + docs (EXECUTED 2026-08-12).** `siem/PtkSiemReceiver/README.md` now covers per-OS separate-account installation, full configuration, retention/capacity, SQLite online backup, implemented witness-reconciliation restore, forward-only migration policy, threat-model rows 1/6/7/8/10/11, network/dependency inventory, and patch cadence. The two-host-equivalent isolated producer→receiver proof used real shipped publishes and a real `ptk_invoke`; exact host evidence is in `.agents/machines.md`.
- **S8 — Packaging (IMPLEMENTED; three-OS CI green 2026-08-12; five-RID draft pending).**
  `siem/build-package.ps1` is the one local/CI/release layout path for the five
  supported RIDs. It stamps the requested version and source commit, includes
  the operator guide, project license, vendored-proto license, and exact
  `VERSION`, while `siem/verify-package.ps1` independently checks those bytes,
  assembly/informational versions, executable mode, and no-config fail-closed
  startup. Ordinary CI builds/verifies the native package on Ubuntu, Windows,
  and macOS; release CI continues to sign and archive it separately on all five
  native runners. Local osx-arm64 proof and both version/doc omission mutants
  pass/fail as intended. Hosted run `31650818998` passed all six jobs at
  `b94bae5`, including native package verification on Ubuntu, Windows, and
  macOS. S8 closes only after the five-RID signed draft workflow is green. MSI
  optional-component authoring remains a separate later plan and is explicitly
  out of scope.

### S3H implementation contract

1. **Scope and protected manifest.** Add a SIEM-local protected-path boundary; do not share or import PTK runtime code. It covers the absolute `PTK_SIEM_CONFIG` file, ingest certificate and private key, every client-CA bundle, optional operator HTTPS certificate and key whenever configured, the absolute SQLite database path, derived `<database>-wal` and `<database>-shm` names, and the deduplicated immediate parents/data directory. Reject relative configured paths. The custody checkpoint and optional file-drop anchor are absent through S3 and remain out of scope; their owning later slice must add them to this manifest before first use.
2. **Protected config read comes first.** Before parsing the operator token or any referenced path, open the config through a platform no-follow boundary, verify that the retained object is the expected regular file with a protected immediate parent, enforce the frozen size bound while reading from that retained object, and verify its stable file identity before and after the read. On Linux/macOS, use non-following open plus `lstat`/`fstat` device-and-inode comparison; on Windows, retain a `CreateFile` handle opened with `FILE_FLAG_OPEN_REPARSE_POINT` and compare reparse attributes, file identity, and final path. A replacement must either leave the originally verified bytes in use or fail startup; it must never cause replacement bytes to be accepted. Existing config permissions/DACLs are never changed.
3. **TLS inputs are identity-stable bytes, not reopened paths.** After config parsing and before certificate construction, apply the same non-mutating protected read to every configured PEM role and parent. The active ingest certificate/key and CA loader consumes those verified bytes (`CreateFromPem`/collection byte or character APIs) rather than reopening a path after validation, and clears private-key/config byte buffers when ownership ends. Optional operator HTTPS paths are protected and securely read even though S5 has not yet added that listener; S3H does not pull that listener or its remaining certificate-suitability behavior forward. Each input has an explicit size cap, and wrong-kind or empty-input failures remain sanitized startup errors.
4. **SQLite is protected around creation and WAL startup.** Before SQLite opens, refuse linked/reparse components and insecure existing database/`-wal`/`-shm` objects, and verify the existing data directory without mutating it. Snapshot which artifacts were absent, atomically precreate each absent database/sidecar name owner-only inside that already protected directory, and give those exact identities to SQLite; this is required on Windows because a later pathname DACL retrofit would leave a creation-time exposure. After WAL policy and migrations materialize the live artifacts, reverify all database/sidecar names, types, identities, ownership, and protection before startup can succeed. Existing insecure objects fail without repair or content mutation. Keep the writer open after this boundary so SQLite cannot recreate an unchecked live sidecar before shutdown. The protected parent plus before/after identity checks defend against other principals; do not claim resistance to a malicious actor already running as the receiver identity.
5. **Exact platform policy.** POSIX verification uses numeric effective-UID ownership and exact `0700` directory/`0600` file modes; on macOS it also rejects an extended ACL. Secure creation uses an atomic owner-only create mode, followed by verification. Windows verification is non-mutating for existing objects and requires the current token SID as owner plus a protected DACL containing exactly one non-callback allow ACE for that SID with `FullControl`; receiver-created objects receive that descriptor and are read back for exact verification. Link checks walk every existing lexical component without resolving it and cover dangling links, file symlinks, directory symlinks, junctions, mount points, and other reparse points.
6. **Startup ordering and failures.** Production startup cannot bind either endpoint until config, all configured TLS inputs, and the SQLite pre/post-open boundary have completed. All violations produce stable, sanitized `SiemReceiverConfigurationException` or `SiemReceiverStartupException` codes without emitting the operator token, PEM material, or other secret content. No hardening failure is downgraded to a warning or repaired for an existing operator-provisioned object.
7. **Cross-platform guards.** Data-drive every current file role and immediate-parent role through wrong kind, missing external input, relative path, wrong owner (using an internal deterministic platform seam where CI cannot change ownership; production constructs the native implementation unconditionally), permissive POSIX mode or Windows DACL, leaf and ancestor link/reparse redirects, and a replacement barrier. Real Linux/macOS tests assert numeric modes/owner; macOS tests cover extended ACLs; real Windows tests cover an unprotected/inherited DACL, extra allow or deny ACE, wrong owner where the runner permits it, and actual file and directory reparse fixtures without a Windows skip. SQLite tests prove insecure pre-existing database/sidecars fail untouched, absent artifacts become exactly protected after WAL startup, and post-create sidecar sabotage fails before a listener is reachable. Guard mutations must demonstrate failure when the config pre-read check, stable PEM-byte load, ancestor redirect check, exact DACL rule, or post-WAL sidecar verification is removed.

Each slice lands as its own commit(s) with tests green (AGENTS.md: commit each slice as it lands).

## Acceptance matrix mapping (discovery `:85-95` → this plan)

| Row | Requirement | Where satisfied |
|---|---|---|
| 1 | Threat model + separate service identity | S7 threat-model doc; separate-account packaging/config (Architecture: Separate principal) |
| 2 | Durable-before-`200` | S3 commit-before-ack ordering; S4b both process barriers and discrimination |
| 3 | Duplicate handling (at-least-once) | S3 idempotence; S4a conformance replay; S4b post-restart replay |
| 4 | Event-ID/hash-chain validation | S2/S3 strict validator: SHA-256 `event_hash` recomputation over exact pre-hash bytes + envelope/sequence/attribute-equality checks (one mutation test per check); broken chain ⇒ rejection + durable quarantine |
| 5 | Crash/disk-full/backpressure/restart | S3 failure paths (never false ack); S4b process-kill/restart tests; 503 backpressure |
| 6 | mTLS or equivalent | S2 mTLS ingest endpoint: `RequireCertificate`, custom-root chain policy, EKU/validity/revocation config, CA-bundle rotation tests |
| 7 | Receiver host storage protection | S3H startup-enforces the fail-closed ownership/mode/DACL/link contract for all config, TLS, database, and sidecar paths present through S3; S4b extends it to custody checkpoints/anchors before first use; separate account + 0700/0600 (S7 docs) |
| 8 | Retention + read authorization | Retention service; operator token + loopback default (S5) |
| 9 | Minimum useful queries/alerts | S5 query set; S6 rules |
| 10 | Upgrade/backup/recovery ownership | S4b implements/tests witness-reconciliation restore; S7 documents SQLite backup API, restore, and migration policy |
| 11 | Network-service patch burden | S7 doc: surface inventory (two endpoints), dependency list, update cadence |

## Drafted decision-entry update (S0 — owner lands this)

> ### ACTIVE (2026-07-15): mini-SIEM receiver build authorized — .agents/plans/mini-siem-implementation.md
> **Status:** Owner directed the move from discovery to implementation on 2026-07-15 (durably recorded in the implementation plan's Status/Provenance sections), selecting Option 1 (PTK-maintained receiver) expanded to a separately installed product (receiver, SQLite durable store, query, alerts, web dashboard; cross-platform with later Windows MSI packaging). The owner waived the D1 existing-deployment comparison from the standing recommendation. The OPEN (2026-07-14) mini-SIEM entry's acceptance questions become the build's acceptance matrix via the implementation plan; that entry should be annotated as superseded by this one. Discovery evidence: `.agents/plans/mini-siem-discovery.md` (codex-reviewed, msp-1..9 resolved).

## Non-goals

- No changes to PTK runtime: no edits to `server/PtkMcpServer` production code or `src/` (the proto is referenced read-only by relative path), no OTLP wire/envelope changes, no changes to `server/AUDIT-EXPORT.md` semantics. Shared-tree edits outside `siem/` are limited to: the CI job addition in `.github/workflows/ci.yml`, the S0 decision entry (owner-landed), and **one authorized additive test-only commit under `server/PtkMcpServer.Tests`** (owner-approved before merge) providing (a) the opt-in exporter-test endpoint override for the S2 conformance seam and (b) the golden v1/v2/v3 fixture serializer for the S4 fixture gate — no assertions weakened, no existing test behavior changed when the override is unset.
- S3H has no shared-tree code carve-out: its implementation and tests stay under `siem/`. It does not add or change audit-v3 production, producer fixtures, the custody witness, checkpoint/anchor configuration, query/dashboard behavior, or alerts, and it does not begin S4, S5, or S6.
- No edits to the resilience plan, its branch, or its conformance suite; the receiver plugs into the seam without changing PTK production bytes (the single test-only carve-out above is the only exception).
- No shared runtime library between PTK and the receiver (proto + fixture bytes only).
- No Sentinel/Splunk/Collector adapters, no multi-tenant support, no remote admin plane, no MSI authoring in this plan (S8 gate).
- No daemon/service coupling into PTK sessions — the receiver is an independent product process.

## Verification

- `dotnet test siem/PtkSiem.slnx` — receiver unit + integration + conformance suites (S2-S3, S3H, S4-S6), green on ubuntu/windows/macos CI matrix. S3H's native protection matrix runs on its real target OS; only wrong-owner cases that require unavailable privilege may use the deterministic platform seam, while real POSIX mode/owner, Windows DACL, and Windows reparse checks remain mandatory.
- S4b barrier suite passes against the real receiver and **fails** the deliberately non-durable configuration (discrimination requirement).
- Real-exporter seam activation: a dedicated CI leg launches `PtkSiemReceiver` locally and runs the producer exporter integration tests (`server/PtkMcpServer.Tests/AuditOtlpHttpExporterIntegrationTests.cs`) with the opt-in endpoint-override env var pointed at the live receiver, so the authorized test-only seam is exercised on every CI run rather than merely available; the same tests also run with the override unset in the ordinary battery, pinning identical behavior when unset.
- Fixture-fidelity check: conformance tests byte-compare against the canonical producer-owned fixture files at head and fail closed if absent or divergent — a producer-side contract change breaks the receiver's conformance suite loudly (tracking, never forking, the contract; no receiver-side copies as source of truth).
- Existing battery stays green and untouched, using the exact commands in `.agents/repo-guidance.md:41`: `dotnet test server/PtkMcpServer.slnx`, `Invoke-Pester -Path tests/PwshTokenCompressor.Tests.ps1`, `pwsh -NoProfile -File server/test-handshake.ps1`. A fresh transcripted baseline (SHA + environment recorded) is captured at S1 start; the predecessor's author-reported counts are not baseline evidence (discovery `:19`).
- Manual smoke (EXECUTED 2026-08-12): equivalently isolated producer and receiver child environments with mutual cross-root read/write denial, real HTTPS receiver leg, asynchronous `otlp_http` export, and real `ptk_invoke`. The producer cursor advanced only after acceptance and the receiver dashboard/API exposed the resulting event; exact evidence is in `.agents/machines.md`. The old phrase "anchored mode" is superseded by the shipped asynchronous cursor contract.

## References

- `.agents/decisions.md:298-340` — the OPEN mini-SIEM entry (status `:300-302`, acceptance questions `:329-335`, recommendation `:337-340`).
- `.agents/plans/mini-siem-discovery.md` — matrix `:85-95`, grading rule `:106-112`, identity `:113-121`, barriers `:122-138`, fixtures `:134-138`, non-goals `:150-156`, discrimination `:163-170`.
- `server/AUDIT-EXPORT.md` — transport/ack/anchor contract (valid nonrejecting ack `:87-97`, interface-boundary precedent `:205`).
- `git show 33d6a35:.agents/plans/mcp-resilience.md` — transport `:561-584`, compat fixtures `:785-801`, prohibitions `:853-856`.
- Retained wire and legacy-administration references (read-only; never shared at runtime): `siem/PtkSiemReceiver/Protos/audit_otlp.proto`, `server/PtkMcpServer/Audit/AuditExportCheckpointStore.cs`, `AuditStartupConfiguration.cs`, `SecureAuditStorage.cs`, and `server/PtkMcpServer.Tests/AuditCoreSchemaTestRecords.cs`.
