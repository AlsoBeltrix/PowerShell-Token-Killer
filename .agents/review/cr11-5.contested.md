# cr11-5: case aliases bypass witness independence

**Intake verdict**: DECLINED

The candidate's concrete macOS premise is false for the current .NET runtime:
`Path.GetRelativePath("/tmp/ptk-data", "/tmp/PTK-DATA/witness")` returns
`witness`, so the existing containment test rejects the alias. A matching
configuration test also passed before any repair. A case-folded Linux mount
may deserve a future platform reproducer, but no evidence here demonstrates
the predicted bypass on a supported environment.
