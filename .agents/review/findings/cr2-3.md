# cr2-3: The audit filter rejects schema-valid output requests containing explicit defaults

Severity: MEDIUM. Source: codereview codex (gpt-5.6-sol @ high, standard)
over `61fc838..a10ad50`, 2026-08-10. Status: CLOSED (accepted).

## Evidence

`server/PtkMcpServer/Audit/AuditCallMetadata.cs:315` — the `list` branch
rejected `handle`/`offset`/`maxBytes`/`pattern` on key PRESENCE, and the
`status`/`read` branches did the same for their inapplicable keys, while
`OutputTool` accepts `handle=null`, `offset=0`,
`maxBytes=OutputStore.DefaultReadBytes`, `pattern=null` as effective
defaults (its own rejections are value-based).

## Predicted observable failure

A client that serializes optional defaults —
`{"action":"list","handle":null,"offset":0,"maxBytes":16384,"pattern":null}`
— receives `audit_boundary_invalid` / `[operation not started]` despite
sending values the advertised tool contract accepts.

## What

Value-based inapplicability: `IsAbsentOrNull` / `IsAbsentOrDefaultNumber`
helpers; list/status/read reject only non-default inapplicable values, and
the non-list `session` check tolerates an explicit JSON null. The audit
boundary stays deliberately stricter than the tool for status (which
ignores those fields entirely): non-default values are still refused.

## Guard proof

`AuditCallMetadataTests.Output_capture_accepts_explicitly_serialized_defaults`
(fails against the pre-fix binder by stash-revert; passes restored).
Existing rejection pins retargeted to non-default values (`offset=5`
status, `offset=1` list) so the strictness half keeps its coverage.

## Files changed

- `server/PtkMcpServer/Audit/AuditCallMetadata.cs`
- `server/PtkMcpServer.Tests/AuditCallMetadataTests.cs`

## Reviewer comments

Reviewer: codex/gpt-5.6-sol/high/standard (codex-cli 0.146.1), batch
verification over the four cr2 fixes (orchestrator-chosen batch: one
shared surface, four sequential commits, one disposable worktree at `98083cc` — recorded deviation from per-finding dispatch, mirrored on
the r806 precedent). Reviewed `a10ad501..98083cc6`; both pins matched.
Verdict: **accepted**, guard_confirmed: true, capability_ok: true,
2026-08-10 20:29 UTC.

> Explicit schema defaults are accepted while non-default inapplicable values remain rejected; the guard failed at base and passed at head.
