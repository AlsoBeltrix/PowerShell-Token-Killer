# cr5-5: Concurrent supervisors can publish a token that the serving UI does not recognize

**Severity**: MEDIUM — the required audit UI becomes intermittently inaccessible (401 against the owner-only token file) until takeover or restart.
**Status**: Verified
**Branch**: —
**Commit**: `415c139`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`3b8dff8..3996e6a`, verdict `findings`, capability_ok true, 2026-08-11.

## Evidence
`server/PtkMcpServer/Audit/Web/AuditWebUiService.cs:263` (LoadOrCreateToken) —
token creation is a check-then-act sequence, and line 280's
`File.Move(..., overwrite: true)` overwrites `ui-token` unconditionally
after each contender has generated its own in-memory token. Every
supervisor executes this before attempting the bind (line 106).

## Predicted observable failure
Two supervisors sharing a new audit root start concurrently; both
observe no token, generate different values, and overwrite the file
independently. The bind winner may serve token A while the file holds
token B; requests using the file's token receive 401.

## What
Token creation is not single-writer and is not tied to bind ownership,
so the value the listener authenticates against and the value the
operator reads can diverge.

## Approach
Structurally closed by cr5-1's fix (`ee0ac2f`): token minting moved
behind a successful bind, so only the listener owner ever writes
`ui-token` — check-then-act across contenders no longer exists, because
losing contenders execute no token code at all. The serving UI holds
exactly the token it published, by construction. This commit contributes
the finding's own guard.

## Files changed
- `server/PtkMcpServer.Tests/AuditWebUiTests.cs` — standby guard.

## Guard proof
- `AuditWebUiTests::A_bind_failed_standby_never_writes_the_token_file`
  — with a foreign process holding the port, the service must stand by
  without creating `ui-token`. FAILS against the pre-cr5-1 revision of
  the service file (`git checkout ee0ac2f~1 --` revert proof: old code
  minted before binding), PASSES at head (proved 2026-08-11).

## Coder dispute (if any)
None.

## Known gaps
None. The predicted failure (serving token ≠ file token) is covered by
the combination of this guard and cr5-1's rotation guard: the file is
written only by the process that serves it.

## Reviewer comments
Reviewer: codex / gpt-5.6-sol / high / standard
codex-cli 0.147.0; reviewed `459039a7`, base `45034923`; batch dispatch
(MEDIUMs cr5-4..8); verdict **accepted**, guard_confirmed true,
2026-08-11: the reviewer independently moved mint/publication before
`listener.Start()` (guard FAILED), restored (PASSED); "listener.Start()
precedes token minting and publication, structurally restricting token
writes to the bind winner". **VERIFIED.**
