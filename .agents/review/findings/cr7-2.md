# cr7-2: The operator and ingest credentials can be configured to the same value

**Severity**: HIGH — equal tokens silently expand the read-only dashboard credential into store-writing ingest authority.
**Status**: Fixed (pending verification)
**Branch**: master
**Commit**: (filled in after commit)

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`47fd8e2..1422bec` (R5b), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`siem/PtkSiemReceiver/Web/OperatorEndpoints.cs:83` compares only the
operator token; `Ingest/ReceiverApplication.cs:359` independently
accepts the ingest token; no startup check rejects equal values.

## Predicted observable failure
With both fields configured equal, the operator token authenticates
certificate-less POSTs to `/v1/logs`.

## What
Two credentials with different authority, no rule keeping them
distinct.

## Approach
The configuration loader fails startup with `token_reuse` when an
ingest token is configured and equals the operator token — the
surfaces' authorities must not be collapsible by configuration.

## Files changed
- `siem/PtkSiemReceiver/Configuration/SiemReceiverConfiguration.cs` —
  the equality refusal.
- `siem/PtkSiemReceiver.Tests/` — loader guard.

## Guard proof
- Config with equal ingest/operator tokens must fail `token_reuse`;
  distinct tokens load. FAILS pre-fix (loads fine), PASSES fixed.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
(pending verification round)
