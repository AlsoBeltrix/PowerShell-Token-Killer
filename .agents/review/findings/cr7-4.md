# cr7-4: Time filters compare unnormalized timestamp strings

**Severity**: MEDIUM — a normal RFC 3339 boundary silently excludes same-second evidence (lexicographic `Z` sorts above `.fffffff`), so operators can miss records while the API returns 200.
**Status**: Verified
**Branch**: master
**Commit**: `a7edc20`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`47fd8e2..1422bec` (R5b), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`siem/PtkSiemReceiver/Web/OperatorEndpoints.cs:120` — `from`/`to` bind
caller text directly against the store's fixed-format UTC TEXT columns.

## Predicted observable failure
`from=2026-08-11T12:00:00Z` excludes an event stored at
`2026-08-11T12:00:00.5000000Z`.

## What
Lexicographic comparison of unnormalized caller text against a
canonical storage format.

## Approach
Each boundary is parsed as a timestamp (invalid → 400 `time_filter`),
converted to UTC, and bound in the store's own canonical format —
`to` additionally rounded up to the tick so a second-precision bound
includes its whole second.

## Files changed
- `siem/PtkSiemReceiver/Web/OperatorEndpoints.cs` — boundary parsing.
- `siem/PtkSiemReceiver.Tests/OperatorApiTests.cs` — boundary guard.

## Guard proof
- A `from` at the stored event's whole second must include the
  fractional-second event; garbage input must 400. FAILS pre-fix
  (event excluded / garbage silently matched nothing), PASSES fixed.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
Verification (codex / gpt-5.6-sol / xhigh / frontier, worktree at
`99c5c7f`, 2026-08-11): CONFIRMED. "Bounds are parsed, UTC-adjusted,
and emitted in seven-digit store format; whole-second upper bounds
include the final tick. Offset and subsecond forms normalize correctly,
and bare dates resolve to UTC midnight. Raw-text sabotage excluded the
same-second event and failed the guard; restored guard passed."
