# cr7-5: Uppercase canonical event IDs incorrectly return not found

**Severity**: LOW — a valid uppercase D-format UUID passes validation but 404s against the lowercase stored text.
**Status**: Verified
**Branch**: master
**Commit**: (filled in after commit)

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`47fd8e2..1422bec` (R5b), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`siem/PtkSiemReceiver/Web/OperatorEndpoints.cs:170` —
`Guid.TryParseExact(eventId, "D", out _)` accepts uppercase hex, but
line 183 binds the original route text against the lowercase GUID text
the receiver stores.

## Predicted observable failure
`GET /api/events/<UPPERCASE-UUID>` for an existing event returns 404
`unknown_event`.

## What
Validation parses; the query ignores the parse result and uses the raw
text.

## Approach
Bind the parsed GUID's canonical `ToString("D")` (lowercase) instead of
the route text.

## Files changed
- `siem/PtkSiemReceiver/Web/OperatorEndpoints.cs` — bind normalized ID.
- `siem/PtkSiemReceiver.Tests/OperatorApiTests.cs` — uppercase-detail
  guard.

## Guard proof
- Detail fetch by the uppercase form of a stored event's ID must
  return 200 with the event. FAILS pre-fix (404), PASSES fixed.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
Verification (codex / gpt-5.6-sol / xhigh / frontier, worktree at
`99c5c7f`, 2026-08-11): CONFIRMED. "The parsed GUID is bound in
canonical D form. Binding the raw uppercase route text reproduced the
404 and failed the guard; restored guard passed. Full SIEM suite
passed 285/285 with a clean worktree."
