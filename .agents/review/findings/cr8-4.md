# cr8-4: Retention can destroy the rejected record that proves an unresolved gap

**Severity**: HIGH — the quarantine attempt that opened a still-active gap can be swept, irreversibly losing the claimed hashes and raw bytes an investigation needs while /api/gaps still shows the gap.
**Status**: Fixed (pending verification)
**Branch**: master
**Commit**: `c0b4889`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`b093dea..782d345` (R5c), verdict `findings` (7), capability_ok true,
2026-08-11.

## Evidence
`SqliteIngestStore.cs` — the opening attempt id is computed and
dropped; gaps persist no link to it; both quarantine delete queries
delete unconditionally.

## Predicted observable failure
Age/size sweep removes the opening attempt of an open gap.

## What
The gap row points at nothing durable.

## Approach
Schema v4: `gaps.opening_attempt_id`; `OpenGap` records it; both
quarantine deletes exclude attempts referenced by non-resumed gaps.

## Files changed
- `siem/PtkSiemReceiver/Storage/SqliteIngestStore.cs` — column, link,
  exclusions.
- `siem/PtkSiemReceiver.Tests/` — guard.

## Guard proof
- Open a gap, run an aging sweep: the opening attempt must survive
  while the gap is unresolved, and be sweepable after resume. FAILS
  pre-fix (attempt deleted), PASSES fixed.
- Round 2 (verification reopen): a faithful v3 store with an existing
  unresolved gap must regain its link at migration
  (`A_migrated_pre_v4_gap_regains_its_opening_attempt_link` — schema
  v5 backfills `opening_attempt_id` from the matching chain_gap
  attempt). FAILS without the v5 backfill (link NULL, attempt swept),
  PASSES fixed.
- Round 3 (second verification reopen — v5's MIN(attempt_id) was
  ambiguous across resumed gaps: a non-opening chain_gap attempt
  sharing boot + claimed sequence linked instead of the true opener):
  schema v6 re-links precisely on `received_utc = opened_utc` (the
  opener commits in the same transaction with the same receipt
  instant). Guard
  `The_backfill_links_the_actual_opener_across_resumed_gaps` replays
  the reviewer's exact reproduction with distinct receipt instants.
  FAILS against v5 (gap 2 linked attempt 2, opener attempt 3 swept),
  PASSES fixed.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
(pending verification round)
