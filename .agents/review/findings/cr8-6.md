# cr8-6: Rule configuration hashes are collision-prone (bare newline framing)

**Severity**: MEDIUM — rule names/eventType may embed newlines, so two materially different rule sets can serialize to identical bytes; alerts and custody would then falsely claim no configuration change.
**Status**: Verified
**Branch**: master
**Commit**: `78c0579`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`b093dea..782d345` (R5c), verdict `findings` (7), capability_ok true,
2026-08-11.

## Evidence
`Configuration/SiemReceiverConfiguration.cs` — `ComputeConfigHash`
joins fields with bare `\n`; loader accepts embedded newlines.

## Predicted observable failure
The reviewer's constructed pair: event_match(a,x)+chain_break(b) vs
one event_match with a newline-crafted eventType — same SHA-256.

## What
Delimiter framing over undelimited strings.

## Approach
Hash the `System.Text.Json` serialization of the ordered rule list
(length-framed by construction, versioned by a leading marker).

## Files changed
- `siem/PtkSiemReceiver/Configuration/SiemReceiverConfiguration.cs`.
- `siem/PtkSiemReceiver.Tests/SiemReceiverConfigurationTests.cs`.

## Guard proof
- The reviewer's colliding pair must produce DIFFERENT hashes. FAILS
  pre-fix (equal), PASSES fixed.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
Verification (codex / gpt-5.6-sol / xhigh / frontier, worktree at
`0c2b2c6`, 2026-08-11): CONFIRMED. "Restoring newline framing
reproduced equal hashes for the pinned distinct rule sets; the guard
failed and passed with versioned JSON restored."
