# cr8-7: event_match detail JSON is assembled without escaping event_type

**Severity**: LOW — a validated record whose event_type carries JSON metacharacters stores malformed or attacker-shaped detail JSON (dashboard textContent rendering prevents script execution).
**Status**: Fixed (pending verification)
**Branch**: master
**Commit**: `0c2b2c6`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`b093dea..782d345` (R5c), verdict `findings` (7), capability_ok true,
2026-08-11.

## Evidence
`SqliteIngestStore.cs` — `EvaluateRule` interpolates eventType (and
boot ids etc.) into detail strings; the validator accepts any
non-empty event_type.

## Predicted observable failure
`"x","injected":true,"y":"` as event_type shapes the stored detail.

## What
String interpolation where a serializer belongs.

## Approach
Every detail is built with `JsonSerializer.Serialize` over an
anonymous object.

## Files changed
- `siem/PtkSiemReceiver/Storage/SqliteIngestStore.cs`.
- `siem/PtkSiemReceiver.Tests/AlertPipelineTests.cs` — guard.

## Guard proof
- Ingest a record whose event_type contains a quote; the rule matches
  it; the stored detail must PARSE and round-trip the exact type
  string. FAILS pre-fix (malformed detail), PASSES fixed.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
(pending verification round)
