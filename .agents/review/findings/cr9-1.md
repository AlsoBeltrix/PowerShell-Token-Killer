# cr9-1: Release journaling gate passes with lifecycle records but no call journaling

**Severity**: HIGH — `server.started` alone satisfies both checks, so a packaged server whose call filter stopped journaling calls still passes the release gate.
**Status**: Fixed (pending verification)
**Branch**: master
**Commit**: (filled in after commit)

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`a43e4e4..c9b41c8` (R6), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`server/direct-product-proof.ps1` — the check accepts any nonempty
artifact containing "event_type"; `AuditServerLifecycle` emits
`server.started` independently of call auditing.

## Approach
The gate now requires CALL evidence: a `call.accepted` record, a
terminal `call.completed`, and a record naming the proof's own named
session (`"proof"`), scanned across all artifacts.

## Files changed
- `server/direct-product-proof.ps1`.

## Guard proof
- The check itself is the guard; bite proved by asserting against an
  audit root holding only a lifecycle-shaped record (sabotage: match
  pattern inverted / run detail below). FAILS when call records are
  absent, PASSES against the real journal.

## Coder dispute (if any)
None. (Digest-level reconciliation was considered and deferred: the
three-way call/terminal/session pin already cannot be satisfied by a
call-silent server, and the proof must stay dependency-free.)

## Known gaps
None.

## Reviewer comments
(pending verification round)
