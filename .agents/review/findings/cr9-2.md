# cr9-2: The server setup README still tells operators auditing is disabled

**Severity**: HIGH — operators following the server-specific guide expect no journal and may omit storage/privacy controls while PTK creates a mandatory audit store and refuses invokes when it is unavailable.
**Status**: Fixed (pending verification)
**Branch**: master
**Commit**: `db59531`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`a43e4e4..c9b41c8` (R6), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`server/README.md` §"Audit compatibility status" — "does not open
audit storage… ptk_state reports audit disabled… PTK_AUDIT_ROOT does
not enable producer behavior." All false since R2; the R6 docs pass
fixed the top-level README and AUDIT-EXPORT.md but missed this file.

## Approach
Section rewritten to the restored contract (mandatory fail-closed
journal, default root, env overrides, export leg, UI, sensitive
storage warning) with a pointer to AUDIT-EXPORT.md.

## Files changed
- `server/README.md`.

## Guard proof
Docs-only: `git diff --check`; claims verified against
`Program.cs` / `AuditOptions.CreateDefault` / `AuditExportSettings`.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
(pending verification round)
