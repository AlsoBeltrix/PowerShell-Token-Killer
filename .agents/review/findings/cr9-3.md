# cr9-3: The docs misclassify the active exact-script evidence store as legacy

**Severity**: HIGH — every current `ptk_invoke` persists the exact submitted script under the audit root, but the docs framed exact-script evidence as a legacy-store feature and told operators to protect only the output root and "legacy" stores.
**Status**: Fixed (pending verification)
**Branch**: master
**Commit**: `de24c84`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`a43e4e4..c9b41c8` (R6), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`server/AUDIT-EXPORT.md` legacy section; `README.md` "separately
operated legacy evidence stores". Production registers
`ScriptEvidenceStoreProvider`, every invoke is
`RequiresScriptEvidence`, and `AuditCallContext` publishes the exact
submitted script.

## Approach
The current-contract sections in both files now state that the live
journal persists exact submitted scripts as protected evidence and
that the audit root (and its backups) must be protected as sensitive
data; the legacy sections stay for legacy-store administration.

## Files changed
- `server/AUDIT-EXPORT.md`, `README.md` (server/README.md already
  carries the warning via cr9-2).

## Guard proof
Docs-only: `git diff --check`; claims verified against
`AuditCallMetadata`/`AuditCallContext`/`Program.cs`.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
(pending verification round)
