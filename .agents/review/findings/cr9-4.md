# cr9-4: Receiver response contract omits the authentication failure response

**Severity**: MEDIUM — a client implemented from the operator contract hits an undocumented 401 during token misconfiguration and may classify it incorrectly.
**Status**: Verified
**Branch**: master
**Commit**: `953e2c2`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`a43e4e4..c9b41c8` (R6), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`server/AUDIT-EXPORT.md` response table (200/400/503 only);
`ReceiverApplication.cs` sends unauthorized; `OtlpHttpResponse` = 401
with protobuf `google.rpc.Status` code 16.

## Approach
A 401 row: protobuf UNAUTHENTICATED status; PTK's exporter classifies
it retryable (operator-fixable configuration).

## Files changed
- `server/AUDIT-EXPORT.md`.

## Guard proof
Docs-only: `git diff --check`; verified against `OtlpHttpResponse`.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
Verification (codex / gpt-5.6-sol / xhigh / frontier, worktree at
`770dae5`, 2026-08-11): CONFIRMED. "OtlpHttpResponse emits HTTP 401 with google.rpc.Status code 16, and HttpAuditDestination classifies 401 as retryable. (Focused HTTPS test environment-blocked by the unavailable macOS keychain before any product assertion.)"
