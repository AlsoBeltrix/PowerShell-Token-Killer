# cr9-5: The isolated audit root is not reliably cleaned up

**Severity**: LOW — a mid-proof exception skipped inspection and deletion entirely, and deletion failures were silently swallowed, leaving `.ptk-proof-audit-*` under the operator's HOME.
**Status**: Fixed (pending verification)
**Branch**: master
**Commit**: `770dae5`

Reviewer (generation): codex / gpt-5.6-sol / high / standard over
`a43e4e4..c9b41c8` (R6), verdict `findings` (5), capability_ok true,
2026-08-11.

## Evidence
`server/direct-product-proof.ps1` — audit checks and deletion sat
after the finally; `Kill` had no follow-up wait; `catch { }` hid
removal failures.

## Approach
The journaling checks and deletion moved INTO the finally: a killed
server is waited out before deletion, and a removal failure prints an
explicit WARNING naming the leftover path.

## Files changed
- `server/direct-product-proof.ps1`.

## Guard proof
Behavioral: full proof rerun passes all three journaling checks and
leaves no `.ptk-proof-audit-*` under HOME.

## Coder dispute (if any)
None.

## Known gaps
None.

## Reviewer comments
(pending verification round)
