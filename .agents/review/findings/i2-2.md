# i2-2: README overclaimed the dev-install heal

**Severity**: LOW — docs sent CLI-less machines to an ineffective heal
path (refresh only runs when the install also registered the server).
**Status**: Verified
**Branch**: master (direct)
**Commit**: `ea95d48`

## Evidence / failure
`server/README.md` said "any dev-install install heals an existing hook
entry"; the code gates the refresh on `$registered` (mhi-6 principle:
never (re)install enforcement without verified registration), which is
false when the claude CLI is absent.

## Approach
Docs aligned to the code: the condition is named, and the CLI-less manual
path (`ptk_init.ps1 -Agent claude` after registering by hand) is stated.
The code's conservative gate is kept deliberately.

## Guard proof
Docs-only; no automated guard. Codex re-grade covers it.

## Reviewer comments
Raised by codex (Codex v0.143.0, gpt-5.5, read-only) reviewing
bcb24c1+67fe442, 2026-07-09. Re-grade: see index.
