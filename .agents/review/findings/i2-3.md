# i2-3: a directory at the hook path passed the existence checks

**Severity**: LOW — uncommon filesystem state, but it re-creates the
silent fail-open class the plan exists to kill (`pwsh -File <dir>` fails
like a missing file).
**Status**: Verified
**Branch**: master (direct)
**Commit**: `69a2b13`

## Evidence / failure
`scripts/ptk_init.ps1`: bare `Test-Path` at the installed-copy selection
and the registered-target staleness scan — a DIRECTORY named
`ptk-hook.ps1` would be selected/blessed while failing open at runtime.

## Approach
`-PathType Leaf` at both checks.

## Guard proof
- `tests/PwshTokenCompressor.Tests.ps1::treats a directory at the
  registered target as stale` — FAILS with the bare Test-Path reinstated;
  PASSES with the fix.

## Reviewer comments
Raised by codex (Codex v0.143.0, gpt-5.5, read-only) reviewing
bcb24c1+67fe442, 2026-07-09. Re-grade: see index.
