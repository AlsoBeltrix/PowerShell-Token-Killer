# r6x-2: three `Windows_*` real-composition tests fail for a non-audit reason

**Severity**: HIGH — these are three of the six real-apphost Windows identities
R6's acceptance matrix must prove (`.agents/plans/mcp-resilience.md`, R6
sub-slice 6). While they fail, R6 cannot be claimed cross-platform complete and
R7 cannot start on a proven Windows baseline. Raised further by the #1
diagnosis below: that one is a live correctness defect, not a test problem — an
ambiguous reset leaves the session projecting usable, so work can be dispatched
into a session whose outcome is unknown.
**Status**: Open — **#1 fixed and verified** at `0848e36` (pending reviewer
verdict); #2 and #3 reproducible but still undiagnosed
**Branch**: `feature/mcp-resilience-r1` (direct, per this repo's recorded
resilience-sequence practice; owner authorised the fix in-session 2026-07-25)
**Commit**: `0848e36` for #1. Review base `7331270`, head `0848e36`.

## Evidence

All three fail on `NETWATCH-01` at head `168905c` (Windows NT 10.0.26200.0 x64,
.NET SDK 10.0.302, ordinary identity), and all three pass on Linux ARM64 at the
same head. Guardian is 480/484 with exactly these three plus `r6x-3`.

1. `ProductionGuardianCompositionTests.Windows_composition_requires_explicit_repair_after_ambiguous_reset`
   — `Assert.Equal` expected `RecoveryUnknown`, actual `Ready`, at
   `ProductionGuardianCompositionTests.cs:1223`.
2. `ProductionGuardianCompositionTests.Windows_composition_never_replays_a_real_effect_when_the_host_dies`
   — `System.Threading.Tasks.TaskCanceledException` after ~1 minute, at
   `ProductionGuardianCompositionTests.cs:2138`.
3. `ProductionGuardianCompositionTests.Windows_composition_keeps_a_real_job_tombstone_and_sealed_output`
   — "The background job did not publish a sealed terminal. Last status: job 1:
   exited 0, output completeness unknown, ... recovery=unavailable: output
   capture unavailable", at `ProductionGuardianCompositionTests.cs:1865`.

## Predicted observable failure

Already observed — these are live failures, not a predicted risk. What is not
yet established is the mechanism, and whether the three share one.

## What

These were originally lumped into `r6x-1` on the theory that one degraded audit
subsystem explained all four `Windows_*` composition failures. Fixing `r6x-1`
disproved that: its sibling `..._classifies_real_prewrite_loss` now passes,
because that one genuinely did fail through `audit=unavailable`. These three
kept their original signatures — a wrong session state, a timeout, and an
unavailable output capture — none of which is the audit-degradation signature.
So one or more separate Windows-specific defects remain.

## Diagnosis of #1 (ambiguous reset) — root cause found 2026-07-25

**An ambiguous session outcome is silently erased by the replacement host's
declared-session restoration, permanently.** It is not a timing race in the
test.

Measured on `NETWATCH-01` at head `3ecc86f` with a throwaway probe that keeps
polling `ptk_state` after the point where the test asserts. Across 61 samples
spanning 6 seconds the session never changes:

```
t0  hostReady=True hostGen=2 sess=Ready warmLost=True boot=Restored
...
t60 hostReady=True          sess=Ready warmLost=True boot=Restored
```

`boot=Restored` is the tell: `BootstrapState.Restored` is only ever written by
the Ready branch of `FrozenDefaultSessionState.ObserveSessionLifecycle`
(`FrozenDefaultSessionState.cs:335-353`, committed at `:391-394`). So the
sequence is:

1. The ambiguous `ptk_reset` makes `GuardianHostSupervisor` call
   `ObserveSessionRecoveryUnknown` (`GuardianHostSupervisor.cs:2431` or
   `:3232`), which sets the alias to `RecoveryUnknown`, `ReadyForEffects=false`,
   `BootstrapState=Unknown` (`FrozenDefaultSessionState.cs:398-414`).
2. The lost host is replaced, and the replacement's declared-session
   restoration emits a **Ready** `SessionLifecycleEvent`.
3. `ObserveSessionLifecycle`'s Ready branch overwrites `State`,
   `ReadyForEffects` and `BootstrapState` unconditionally. Nothing consults
   whether the alias is sitting in an unrepaired `RecoveryUnknown`.
4. Nothing ever restores it, so the alias projects `Ready` and
   `ReadyForEffects=true` for the rest of the session.

Confirmed structurally: `RecoveryUnknown` appears in
`FrozenDefaultSessionState.cs` at exactly two lines — the method at `:398` and
its assignment at `:409`. There is no stickiness, no repair gate, and no
guard on the Ready path.

**Why this matters beyond a red test.** The snapshot type enforces
`readyForEffects == (state == Ready)`
(`PtkSharedContracts/Public/PublicState.cs:152`), so the alias does not merely
look wrong — it is projected as fully usable. R6's contract is that ambiguity
becomes `recovery_unknown` and that ordinary session work stays blocked until
an explicit lifecycle result repairs the alias
(`.agents/plans/mcp-resilience.md`, R6 sub-slice 5;
`GuardianHostSupervisorContracts.cs:206-212`). A model can therefore dispatch
work into a session whose reset outcome is unknown — the exact no-replay
boundary the design exists to protect.

**Inferred, not proven: macOS passes by ordering luck rather than by
protection.** The product has no guard on either platform; the plausible
reading is that on macOS the ambiguous marking happens to be committed after
the ready lifecycle and therefore survives, while on Windows the replacement
completes first and overwrites it. Confirming that needs the same probe run on
macOS, which has not been done. Either way the invariant is unenforced
everywhere, so this should not be treated as Windows-specific behaviour.

## Approach

For #1, not yet authored. The shape is to make an unrepaired `RecoveryUnknown`
**sticky** in `FrozenDefaultSessionState`: a Ready lifecycle event arriving for
an alias in that condition must not silently restore it to usable, and only an
explicit repair (reset/reopen) may clear it. The care needed is in not
breaking the legitimate `RecordAcknowledgedReady` initial-ready path or the
automatic-recovery ready path, both of which also arrive as Ready lifecycles.

For #2 and #3, still undiagnosed. Both fail deterministically (2 of 2 isolated
runs each at `3ecc86f`), so neither is a flake and both are directly
reproducible. Do NOT assume they share #1's cause: #1 turned out to be a
guardian declared-state ordering defect, whereas #3's "output capture
unavailable" points at the supervisor-owned output store and #2 is a plain
60-second timeout. Treating the group as one bug is what produced the original
`r6x-1` mis-attribution.

## Files changed (#1 only)

- `server/PtkMcpGuardian/Standalone/FrozenDefaultSessionState.cs` —
  `AliasState.AmbiguousUntilRepaired`, armed in
  `ObserveSessionRecoveryUnknown`, cleared only in
  `ObserveSessionOperationResult`, and honoured by an interception in
  `ObserveSessionLifecycle` placed **after** the Ready event's validation so a
  malformed Ready event still throws exactly as before
- `server/PtkMcpGuardian.Tests/FrozenDefaultSessionStateTests.cs` — the two
  guards below

Nothing else changed. #2 and #3 are untouched.

## Guard proof (#1)

The pre-existing `Ambiguous_lifecycle_stays_blocked_until_an_authoritative_repair`
covered only `ObserveHostReady` — **not** the `SessionLifecycleEvent` channel
that actually carries a replacement host's restored session. That is why the
defect shipped past a test whose name claims to cover it. Two new guards close
the gap, and both are platform-neutral, so unlike `r6x-1` this proof runs
anywhere:

- `Ready_lifecycle_cannot_repair_an_ambiguous_alias` — a Ready lifecycle leaves
  the alias blocked, still absorbs the new worker identity, and an
  authoritative result then repairs it.
- `Repaired_alias_accepts_an_ordinary_ready_lifecycle_again` — stickiness ends
  at the repair, so automatic recovery can still bring the alias back.

Mutation-proven on macOS, each mutation reddening only its own guard:

| mutation | red |
|---|---|
| interception removed | `Ready_lifecycle_cannot_repair_an_ambiguous_alias` |
| flag never armed | same |
| repair never clears the flag | `Repaired_alias_accepts_an_ordinary_ready_lifecycle_again` |

Baseline and restored runs both green (3/3).

## Verification (#1)

- **The real Windows composition test passes**, twice:
  `Windows_composition_requires_explicit_repair_after_ambiguous_reset` on
  `NETWATCH-01` at `0848e36`, `Failed: 0` on both runs. This is the end-to-end
  proof on the real apphost, not just the unit guards.
- Windows Guardian went **480/484 → 484/486**: the ambiguous-reset identity is
  closed and the two new guards pass there too. The two remaining failures are
  this finding's #2 and #3, untouched by design.
- macOS at `0848e36` is fully green: architecture 73/73, Guardian 486/486,
  server 2,039/2,039, Pester 141 with two expected skips, complete handshake.
- `r6x-3` happened to pass in that Windows run. That is its ~1-in-5 pass, not
  a repair — it is unrelated to this fix and stays open.

## Coverage gap noticed while fixing this

The ambiguous-reset contract has **no Unix composition test** — only
`Windows_composition_requires_explicit_repair_after_ambiguous_reset` exercises
it end to end. Combined with the diagnosis (the guard was missing on every
platform, so macOS passed by ordering luck), that means this contract was
effectively unproven off Windows. The two new platform-neutral unit guards
partly close it; a Unix composition test for the same contract is worth a
scoped follow-up.

## Coder dispute

None.

## Known gaps

- Diagnosis requires a Windows host: these are real-apphost tests and do not
  reproduce on macOS or Linux.
- Whether these are regressions introduced during R6 or never worked on Windows
  is unestablished. Prior records show Guardian 442/442 green on `NETWATCH-01`
  at `0b3b0de`, but the `Windows_*` composition identities were added during R6,
  so they may simply never have run green on Windows.

## Reviewer comments (#1)

`Reviewer: kimi / kimi-code/k3 (config-resolved default) / harness default / standard`

- kimi-code 0.29.1, `kimi -p … --output-format stream-json`, no `-m` (owner
  instruction: default model/effort). Session
  `session_7314ac84-a161-4b83-9e6c-01261f2c7288`.
- Reviewed head `0848e3664ee18f4441b12c36ab00beb370d54aa4`, base `7331270` —
  both match the dispatch.
- `guard_confirmed`: **true**, and unlike `r6x-1` this is the strong sense. The
  reviewer ran the mutation proof itself in its own detached worktree (removed
  afterwards, coder's tree untouched), restoring with an explicit mtime touch to
  defeat the stale-binary trap, and **independently reproduced all three**
  mutation results. Then ran the full entry point: architecture 73/73, Guardian
  486/486, server 2,039/2,039.
- **Verdict: accepted**, 2026-07-25. Orchestrator-computed: verdict in enum,
  both SHAs match, `guard_confirmed` literally true. Zero defects raised.

Two points where it verified something the fix had only reasoned about:

1. **Clearing only in `ObserveSessionOperationResult` is provably the explicit
   repair channel.** Its sole production caller
   (`GuardianHostSupervisor.cs:3224`) is gated on a correlated success terminal,
   and `IsSessionChangingOperation` (`:3611-3616`) covers only
   Reset/SessionOpen/SessionClose/SessionRestart — all explicit operator
   actions, none emitted by automatic recovery. So "authoritative
   session-changing result" and "explicit repair" are the same set here, and
   clearing unconditionally is right because a known outcome ends ambiguity by
   definition.
2. **Absorbing the worker identity while blocked is not merely safer — the
   alternative wedges.** Ignoring the Ready event entirely would leave
   `state.WorkerIdentity` naming the dead worker, and
   `ObserveSessionOperationResult`'s binding check (`:441-447`) would then throw
   on the repair result targeting the live worker, blocking the alias
   permanently.

Also confirmed: the interception sits after all lifecycle validation, so a
malformed Ready event still throws (read, not assumed); Cold/Faulted are not a
hole, since neither touches the flag and any later Ready is still intercepted;
no stuck/wedge path exists; and the pre-existing
`Ambiguous_lifecycle_stays_blocked_until_an_authoritative_repair` still means
what it claims — it covers the `ObserveHostReady` channel, which was never the
hole.

One non-blocking observation, recorded not acted on: `R3FakeGuardianComposition`
has no equivalent stickiness, but its `ObserveSessionLifecycle` is a no-op so it
cannot exhibit the defect.
