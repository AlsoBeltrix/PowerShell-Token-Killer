# sd1-5: expandable-string blanking erases executable nested code (missed detection)

**Severity**: LOW — a miss, not a false positive; the script is unparsable
either way and the user gets PowerShell's parser error instead of the
planned process-substitution guidance.
**Status**: DECLINED at intake — inside sd1-3's recorded known gap and the frozen precision-first rule
**Branch**: n/a (no work)

## Evidence
Reported by codex at re-grade round 1 (head `acb0f39`): blanking the full
`StringExpandable` extent also removes its executable `$()` nested tokens.
**Master verified live at head:** `echo "$(diff <(sort a) <(sort b))"`
(bash-valid, two pwsh `RedirectionNotSupported` errors) returns null.

## Why declined
sd1-3's recorded known gap states the accepted direction: "blanking only
ever removes evidence, so degradation is toward a miss (today's behavior),
never an over-match." A miss keeps the frozen precision-first rule intact —
the user sees today's behavior (PowerShell's parse error), which is the
pre-detector status quo, not a wrong refusal. Kept for the record; revisit
only if slice-2+ recall on real transcripts shows this class matters.

(The over-match half of the same blanking mechanism is NOT covered by the
known gap — that is sd1-6, admitted.)

## Reviewer comments
Origin: codex re-grade round 1 new-finding #2 (Codex v0.144.0, gpt-5.6-sol).
