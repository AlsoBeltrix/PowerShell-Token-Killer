# sd2-5: No guard covers route=pwsh consent on the background branch

**Severity**: LOW — coverage gap on a D1-required consent path; production
is currently correct.
**Status**: Verified
**Branch**: master (direct, per recorded precedent)
**Commit**: `32f181b`

## Evidence
`server/PtkMcpServer.Tests/ShellDialectWiringTests.cs` tests route=pwsh
foreground only and raw=true background only. Nothing pins that (a) route
normalization sits ABOVE the background branch and (b) the background
check honors route=pwsh.

## Predicted observable failure
A regression moving normalization back below the background branch (its
pre-slice-2 position) or dropping the route check would refuse a consented
`background=true, route="PWSH"` call and start no job — with the suite
green.

## What
The two background-bypass consents are half-guarded: raw yes, route=pwsh
no; mixed case doubles as the normalization-placement probe.

## Approach
Add a background test with route:"PWSH": no refusal, job 1 started, one
JobManager entry. Red leg: temporarily re-inline normalization below the
background branch — the new test must fail; restore — pass.

## Files changed
- `server/PtkMcpServer.Tests/ShellDialectWiringTests.cs` — new
  Background_route_pwsh_bypasses_detection_and_starts_the_job
  (route:"PWSH", background:true → no refusal, job 1 started, one entry).

## Guard proof
- Live red/green run 2026-07-09: the exact predicted regression was
  injected (route normalization moved back below the background branch,
  its pre-slice-2 position) → the new test FAILS; production code
  restored → PASSES (class suite 14/14).

## Coder dispute (if any)
None — admitted; confirmed by inspection of the test file.

## Known gaps
None.

## Reviewer comments
codex (codex-cli 0.144.0, read-only), reviewed 8c234e8 (base 3121fc0),
2026-07-09/10 UTC: initial-pass finding (LOW), evidence and predicted
failure as above.

Re-grade: codex (codex-cli 0.144.0, read-only), reviewed 79b63fb (base
712939b), 2026-07-10T01:38Z — **RESOLVED**: "The mixed-case background
route PWSH guard proves normalization precedes detection, consent
bypasses refusal, and a job starts."
