# sd4-2: Nudge guard does not require the advertised bash wrapper

**Severity**: LOW — regression protection only; the emitted text is
currently correct.
**Status**: In progress
**Branch**: master (direct, per recorded precedent)
**Commit**: (filled in after commit)

## Evidence
`tests/PwshTokenCompressor.Tests.ps1` nudge assertions match
'PowerShell 7, not bash' and 'where bash exists' separately; a nudge
edit dropping the concrete `bash -lc '...'` wrapper while keeping both
phrases stays green.

## Predicted observable failure
Installed nudges could silently lose D3's concrete recovery command with
the battery green.

## What
The guard under-specifies the D3 line relative to the hook-deny guard,
which pins the full qualified phrase.

## Approach
Assert the exact qualified phrase `bash -lc '...' where bash exists`
(mirroring the hook assertion) plus 'translate bash-only'.

## Files changed
- `tests/PwshTokenCompressor.Tests.ps1` — strengthened nudge assertions.

## Guard proof
Red leg: with `bash -lc '...'` removed from the nudge block (keeping the
other phrases), the strengthened assertion FAILS; restored, PASSES.

## Coder dispute (if any)
None — admitted.

## Known gaps
None.

## Reviewer comments
codex (codex-cli 0.144.0, read-only), reviewed 8bb96b1 (base f0f3238),
2026-07-10 UTC: initial-pass finding (LOW).
